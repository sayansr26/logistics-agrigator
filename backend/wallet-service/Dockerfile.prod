# =============== PRODUCTION DOCKERFILE ===============
FROM node:18-alpine

WORKDIR /app

RUN apk add --no-cache openssl bash
RUN npm install -g pnpm@8.15.1

COPY pnpm-workspace.yaml pnpm-lock.yaml package.json ./
COPY shared ./shared
COPY backend/wallet-service/package.json ./backend/wallet-service/

RUN cd backend/wallet-service && pnpm install --prod --frozen-lockfile --ignore-scripts

COPY backend/wallet-service ./backend/wallet-service

# Copy shared into service directory for relative imports
RUN cp -r /app/shared /app/backend/wallet-service/
RUN cd backend/wallet-service && npx prisma generate

# Create non-root user first
RUN addgroup -g 1001 -S nodejs && adduser -S nodejs -u 1001

# Copy production entrypoint script for automated migrations
COPY --chown=nodejs:nodejs scripts/production-entrypoint.sh /app/production-entrypoint.sh
RUN chmod +x /app/production-entrypoint.sh
RUN mkdir -p /app/logs/wallet-service && chown -R nodejs:nodejs /app/logs
RUN chown -R nodejs:nodejs /app

USER nodejs
WORKDIR /app

EXPOSE 3006

ENV NODE_ENV=production
ENV PORT=3006
ENV SERVICE_NAME=wallet-service

ENTRYPOINT ["/app/production-entrypoint.sh"]
CMD ["node", "backend/wallet-service/server.js"]
